# stm_sd 板级示例

示例入口展示 `XCZSDNAND4GAS + SDMMC2` 的显式绑定和单块读回校验。它需要宿主工程提供 CubeMX 生成的 `main.h`、`sdmmc.h`、HAL tick 和 `hsd2`；不会自动加入 `stm_sd` 库目标。

示例访问启动后的第 8 个逻辑扇区。使用前必须确认该扇区属于专用测试区域并已完成备份；生产系统应改为只读身份和容量检查。
